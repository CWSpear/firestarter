language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-firestarter' ]; then cd .. && eval "mv $currentfolder generator-firestarter" && cd generator-firestarter; fi

